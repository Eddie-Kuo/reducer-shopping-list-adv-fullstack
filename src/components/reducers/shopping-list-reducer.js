export const initialState = () => {
  return {
    shoppingList: [
      { id: '1', item: 'apples' },
      { id: '2', item: 'bananas' },
    ],
  };
};

export const reducer = (state, action) => {
  return state;
};
